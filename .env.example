# MongoDB Database Connection
# Get your connection string from MongoDB Atlas or your MongoDB instance
# Format: mongodb+srv://username:password@cluster.mongodb.net/database-name?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/quiz-grid?retryWrites=true&w=majority

# Session Secret
# Generate a random string for session encryption (use a strong random string in production)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Server Port
# Default port for the server (Vercel will override this)
PORT=3000

# Environment
# Set to 'production' for production, 'development' for local development
NODE_ENV=development
